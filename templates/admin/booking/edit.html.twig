{% extends "admin/base.html.twig" %}

{% block title %}Modification de la réservation {{ booking.id}}{% endblock %}

{% block body %}

   <div class="container">
      <h1 class="mt-5">Modification de la réservation {{booking.id}}</h1>

      <div class="alert alert-info">
         <strong>Annonce liée : </strong> {{booking.id}} <br>
         <strong>Visiteur : </strong> {{booking.booker.fullName}} <br>

      </div>

      {{ form_start(form) }}

      {{ form_widget(form)}}

      <button type="submit" class="btn btn-primary">Modifier le commentaire</button>

      {{ form_end(form) }}      
   </div>
{% endblock %}